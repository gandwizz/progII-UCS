
#include <stdlib.h>
#include <stdio.h>

void main(){

    FILE * arquivo;
    int i;
    arq = fopen("teste.txt", "rt");

    if(arq == NULL){printf("Erro na abertura!\n");}
    int valor;

    while(fscanf(arq, "%d", &valor) != EOF)
    for ( i = 0; i < 20; i++){
        
    }
    

    fclose(arquivo);
}
